<% content_for :title, "Editar paciente" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">Editar paciente</h1>
    <p class="text-base-content/60 text-sm mt-1"><%= @patient.name %></p>
  </div>

  <div class="card bg-base-100 shadow">
    <div class="card-body">
      <%= render "form", patient: @patient %>
    </div>
  </div>
</div>
