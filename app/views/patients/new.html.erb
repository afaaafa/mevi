<% content_for :title, "Novo paciente" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">Novo paciente</h1>
    <p class="text-base-content/60 text-sm mt-1">Preencha os dados do paciente para cadastrá-lo.</p>
  </div>

  <div class="card bg-base-100 shadow">
    <div class="card-body">
      <%= render "form", patient: @patient %>
    </div>
  </div>
</div>
